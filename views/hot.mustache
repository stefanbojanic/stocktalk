<!doctype html>

<html lang="en">
<head>
    <meta charset="utf-8">

    <title>Stocktalk - Hot Tickers</title>
    <meta name="description" content="Stocktalk">
    <meta name="author" content="Stocktalk">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Fira+Mono:wght@500&family=Fira+Sans&display=swap" rel="stylesheet">

    <style>
        body {            
            font-family: 'Fira Sans', sans-serif;
        }
        table {
            font-family: 'Fira Mono', monospace;
            border-collapse: collapse;
            min-width: 600px;
        }
        .content {
            overflow: hidden;
        }
        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }
        
        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>

</head>

<body>
    {{ > sidebar }}
    <div class="content">
        <h2>Hottest tickers for {{date}}</h2>
        <form action="/">
            <label for="birthday">Date to view:</label>
            <br>
            <input type="date" id="date" name="date">
            <input type="submit">
        </form>
        <br/>
        <table>
            <tr>
                <th>Ticker</th>
                <th>Upvotes</th>
                <th>Posts</th>
                <th>Bull</th>
                <th>Bear</th>
                <th>Neutral</th>
            </tr>
            {{#tickers}}
                <tr>
                    <td>{{ticker}}</td>
                    <td>{{upvotes}}</td>
                    <td>{{count}}</td>
                    <td>{{#toFixed}}{{sentiment.pos}}{{/toFixed}}%</td>
                    <td>{{#toFixed}}{{sentiment.neg}}{{/toFixed}}%</td>
                    <td>{{#toFixed}}{{sentiment.neu}}{{/toFixed}}%</td>
                </tr>
            {{/tickers}}
        </table>
    </div>
</body>
</html>